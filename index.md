# Bandcamp Wishlist Year Filter

在 Bandcamp 收藏夹（wishlist）页面搜索框右侧添加「发行年份」过滤器的 Tampermonkey 用户脚本。支持按发行年份区间过滤整张收藏列表，解析结果按账号永久缓存，上千条的收藏也能分几次访问全部补齐。

> 又名：Bandcamp 收藏夹年份过滤器（`bandcamp-wishlist-year-filter.user.js`）

---

## 内容介绍

Bandcamp 的收藏夹页面默认不显示发行日期，收藏多了以后很难按年代回顾专辑。本脚本解决两件事：

1. **补全年份**：逐条调用 Bandcamp 站内接口补齐每张专辑的发行年份；
2. **按年份过滤**：在搜索框右侧提供「起始年份 – 结束年份」两个下拉框，随时按年代筛选当前页面条目。

### 功能特性

- 🔍 年份区间过滤（起始 / 结束两个下拉菜单），与页面原生搜索框并排
- 🗂 **与 DOM 解耦的全库解析**：页面默认只渲染前 20 条，脚本会先把整个 wishlist 的条目清单（批量接口分页拉取）存下来，再在后台逐条补年份——即使页面没往下滚，剩余的一千多条也会在后台解析完
- 💾 **账号绑定永久缓存**：解析结果按 `fan_id` 分区写入油猴 `GM_setValue`（不可用时回退 `localStorage`），刷新页面、关闭浏览器都不丢，除非退出登录，否则不重置
- ⏱ **限流自保护**：所有站内请求统一经过限流器（默认 2 次/秒 + 滑动窗口），收到 429 自动指数退避（8s → 120s，优先采用 `Retry-After`），冷却后自动续跑，被限流打断的条目不会被误记成「无年份」
- 🛡 **自愈加固**：不完整的条目清单不会被当成完整缓存（带续拉游标持续补建）；后台 worker 单条异常不中断整轮；看门狗定时巡检自动重启停滞的 worker
- 🚀 **按年份过滤时自动补全条目**：选择非最晚年份后，页面里没渲染出来的那部分会被脚本自动点 Bandcamp 的「view all」并等待其自己分页加载拉进 DOM（不再主动滚动，避免与 Bandcamp 自身的分页加载冲突），原解析缓存直接落到这些新 li 上、不会重新请求远端
- 🔄 工具栏提供「清除年份」「重建清单」两个按钮作为手动逃生口（前者只清年份缓存、保留条目清单；后者只清条目清单、保留已解析年份——互补关系）
- 📍 每个条目右下角显示年份角标，未知年份半透明提示
- 🛰 **零跨域请求、零数据上报**：只访问 `bandcamp.com` 站内接口，隐私友好

### 界面示意

```
[发行年份] [起始年份▼] – [结束年份▼] [重置] [清除年份] [重建清单]  全库 1100/1238 · 清单 1238 · 加载条目 320/1238（目标年份 50/87）
```

- 状态栏「全库 N/M」= 已解析条数 / wishlist 总条数（后台全库进度）
- 状态栏「· 加载条目 N/M（目标年份 x/y）」= 当前选了非最晚年份、页面里这个年份还没全渲染，脚本正在自动点「view all」并等待 Bandcamp 自己分页加载
- 状态栏「· 已显示 x/y 张」= 该年份在页面里已可见的条数（用户手动下拉加载时 x 会实时上涨）
- 「· 清单 K（补建中）」= 条目清单还没拉全，正在后台续拉
- 冷却期间显示倒计时（如「429 冷却 32s 后自动续跑」）

---

## 如何安装

### 环境要求

- 浏览器 + 用户脚本管理器：**Tampermonkey**（推荐，Firefox / Chrome / Edge / Safari 均可），或兼容的 Violentmonkey / Greasemonkey
- 无需安装任何第三方库，纯原生 JS

### 安装步骤

1. 安装 [Tampermonkey](https://www.tampermonkey.net/)（如未安装）；
2. 打开本仓库的 `bandcamp-wishlist-year-filter.user.js` 原始文件页面；
3. Tampermonkey 会自动弹出安装确认，点击「安装」；
4. 也可以手动方式：Tampermonkey 面板 →「新建脚本」→ 清空内容 → 粘贴本文件全部代码 → Ctrl+S 保存。

> 安装时如弹出 `GM_setValue / GM_getValue / GM_deleteValue` 权限提示，**必须勾选允许**，否则退化为 `localStorage` 存储（刷新缓存仍可用，但可靠性弱于油猴存储）。

### 版本

当前版本 **v1.3.9**。

### 版本日志

- **v1.3.9**（2026-09）：三处交互/体验修正。① 过滤隐藏改用 `position:absolute` 移出视口（此前 `height:0` 在 Bandcamp 的 grid/flex 条目容器下仍占一个网格单元格，下拉时会留大量空白；`absolute` 让其余条目自然补位相邻排布，且 `offsetWidth` 仍 > 0，`:visible` 照常匹配不触发报错）。② 新增 `syncAutoLoadProgress`：手动下拉加载出新条目后「已显示 x/y」实时变化，不再卡旧值；view all 按钮消失后改提示「页面已无可加载条目」而不是继续让你点它。③ 工具栏按钮「清除缓存」更名「清除年份」，与「重建清单」区分开——前者只清年份缓存（清单保留），后者只清条目清单（年份保留），互补而非包含。
- **v1.3.8**（2026-09）：**修复「view all 加载不出结果」的最终病根**。此前用 `display:none` 隐藏被过滤的 li，而 Bandcamp 的 view all / 滚动分页 ajax 完成回调用 jQuery `:visible`（判据 `offsetWidth > 0`）扫描已渲染条目——全部不可见时一个都选不到，走到 reject 分支但 `completeCallback` 未设 → 抛 `e.completeCallback is not a function`，新 li 也不插入 DOM。现改为 `height:0 + opacity:0 + overflow:hidden`：用户看不到、不占布局空间，但 `offsetWidth` 仍 > 0，`:visible` 照常匹配。
- **v1.3.7**（2026-09）：停止在自动加载循环里 `scrollToBottom()`。Bandcamp 点 view all 后会自己分页加载完，我们再滚动到底会额外触发 `onScroll → paginate → getItems`，两条并发加载流打架导致同样的报错。改为纯轮询等待。
- **v1.3.6**（2026-09）：`findViewAllEl` 改为钻取 `div.expand-container` **内部的** `<button>`/`<a>`——click handler 绑定在内部元素上，点外层 div 时 `event.target` 不对会走错分支。
- **v1.3.5**（2026-09）：view all 触发方式加固（优先 jQuery `trigger`，其次 `dispatchEvent` 模拟完整鼠标序列）；新增报错自动降级——检测到 `completeCallback` 报错后切换为手动模式，状态栏提示用户手动点击。
- **v1.3.4**（2026-09）：autoLoad 期间改用 rootEl 的 `visibility:hidden` 整体隐藏，`applyFilter` 顶部加 `autoLoad.running` 守卫。
- **v1.3.3**（2026-09）：修复「切到非最晚年份后页面为空」——脚本检测到所选年份在 DOM 里没渲染全时，会自动触发 Bandcamp 自带的「view all + 滚动懒加载」把剩余 li 拉进 DOM，原解析缓存即时落到新条目上，无需额外网络请求。
- **v1.3.2**：发行年份下拉菜单改为从全库解析缓存 `store.data` 取值，而不仅是当前 DOM 中的 20 张专辑；自此刷新页面后台也能持续推进。
- **v1.3.1** 起加上自愈、watchdog 与「重建清单」按钮，稳固大列表场景。

---

## 如何使用

1. 登录 Bandcamp，打开你自己的收藏夹页面，例如 `https://bandcamp.com/<用户名>/wishlist`；
2. 页面加载后，搜索框右侧会出现「发行年份」过滤器控件；
3. **按年份过滤**：在左侧下拉选择起始年份（含），右侧选择结束年份（含），页面条目即时过滤；点「重置」清除过滤；
4. **自动补全条目**：选了非最晚年份后，状态栏出现「加载条目 N/M（目标年份 x/y）」——脚本会自动点击 Bandcamp 的「view all」把页面里没渲染出来的 li 加载进来，然后**等待它自己加载完**（不再主动滚动，避免与 Bandcamp 自身的分页加载冲突）。新 li 直接读取已缓存的年份，不会重复请求远端。加载期间若仍不够，状态栏会提示你手动点一下「view all」；
5. **等待全库解析**：状态栏显示「全库 N/M」实时进度。后台会以约 2 条/秒的速度逐条补齐年份并即时落盘。**不用一直开着页面等**——随时刷新或关闭，下次打开会自动续跑；
6. 想立刻看到更早年代的年份出现在下拉菜单里？无需手动加载全部条目，后台解析完自然就会出现——两个下拉框的选项**跟随全库解析进度动态刷新**；
7. 遇到异常可手动干预：
   - **重建清单**：丢弃缓存的条目清单（**保留已解析年份**）重新拉取，用于修复「进度长期不动」；
   - **清除年份**：清空当前账号全部年份缓存并重新联网解析（**条目清单保留**，两个按钮互补而非包含）。

### 状态栏速查

| 显示 | 含义 |
| --- | --- |
| `已解析 20/20` | 当前页面 DOM 中条目的解析进度 |
| `全库 980/1238` | 整个 wishlist 的解析进度（清单已拉全） |
| `全库 400/1238 · 清单 723（补建中）` | 清单还在后台续拉，会持续涨 |
| `加载条目 320/1238（目标年份 50/87）` | 选了非最晚年份，页面里这个年份还没全渲染，脚本正在自动点 view all 并等待加载 |
| `已显示 50/87 张，下拉或点「view all」加载更多` | 页面还没渲染出该年份的全部条目，继续下拉数字会实时上涨 |
| `已显示 50/87 张（页面已无可加载条目）` | view all 按钮已消失，Bandcamp 认为没有更多条目了 |
| `429 冷却 32s 后自动续跑` | 触发限流，正在退避，无需操作 |

---

## 注意事项

- **账号分区**：缓存按登录账号的 `fan_id` 分区；未登录时使用 `anon` 分区。退出登录后重新登录同一账号，缓存仍在；换账号互不干扰。
- **限流是常态，不是故障**：Bandcamp 对 `tralbum_details` 接口有限流。默认 2 req/s 是稳妥值；若仍频繁触发 429，可把脚本内 `CFG.requestsPerSecond` 调成 `1` 或 `0.5`（不建议调大）。
- **解析需要时间**：上千条收藏按 2 req/s 约需 10 分钟。脚本设计为「随开随续」：每次打开页面都会在后台继续补，不要求一次性盯着页面等完。
- **非官方接口**：脚本调用的是 Bandcamp 页面自身使用的接口（同源），Bandcamp 若调整接口结构可能导致脚本失效，届时以仓库最新版为准。
- **只适用于 wishlist 页面**：脚本通过 `@match bandcamp.com/*` 加载，但只在检测到收藏夹搜索框时注入控件，其他页面不影响。
- **「view all」是 Bandcamp 自己的机制**：脚本只是模拟点击 + 滚到底来触发它；如果你在某个时间段内点了重置、或 Bandcamp 自身渲染逻辑变化，自动加载可能停在「该年份 x/y 张已在页面，其余请点 view all」——再点一下页面自带的「view all」即可，无需其他动作。
- **隐私**：脚本无任何外部请求以外的网络行为——所有请求仅发往 `bandcamp.com` 同源接口；数据只存本机油猴存储 / `localStorage`；不含任何上报、统计、埋点代码。
- **不要把它当爬虫工具滥用**：请以正常浏览速度使用，尊重网站服务条款。

---

## 作者

- **WorkBuddy**（hy4 模型为主）
- 项目主页：<https://github.com/Dennis-Sun/bandcamp-wishlist-year-filter>

---

## 协议与致谢

- **开源协议**：本项目以 [MIT License](./LICENSE) 发布，可自由使用、修改、分发（请保留版权声明）。
- **技术栈 / 借助的工具**：
  - [Tampermonkey](https://www.tampermonkey.net/) —— 用户脚本管理器，提供 `GM_setValue / GM_getValue / GM_deleteValue` 持久化 API
  - Bandcamp 页面站内接口：`/api/mobile/25/tralbum_details`（补发行日期）、`/api/fancollection/1/wishlist_items`（批量条目清单）
  - 测试：`jsdom`（DOM 集成冒烟测试）与 Node.js 纯逻辑回归测试（开发期使用，见提交历史）
- **声明**：本项目与 Bandcamp 无隶属关系，为个人自用工具开源分享；脚本依赖的非公开接口如遇变更，恕不另行通知。

---

*Made with ❤️ by WorkBuddy · hy4*
